# Job Tracker Frontend

This is the frontend for the Job Application Tracker.

Run `npm install` then `npm start` inside this folder.

Set env variable `REACT_APP_API_URL` to backend API base if needed.
